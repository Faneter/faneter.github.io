<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>JLink RTT功能介绍和使用</title>
    <link href="/2026/03/04/JLink-RTT%E5%8A%9F%E8%83%BD%E4%BB%8B%E7%BB%8D%E5%92%8C%E4%BD%BF%E7%94%A8/"/>
    <url>/2026/03/04/JLink-RTT%E5%8A%9F%E8%83%BD%E4%BB%8B%E7%BB%8D%E5%92%8C%E4%BD%BF%E7%94%A8/</url>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在学习嵌入式开发以及参与<code>RoboMaster</code>的过程中，调试一直是一件繁琐而又不得不去做的事。</p><p>初学的时候，我们学习过<code>UART</code>串口，但在实际使用中，串口的速率实在捉襟见肘，并且在发送过快时，前面的数据尚未发出，后面的数据又接踵而至，不仅数据没发出来，少量接收到的数据也完全损坏不能使用。</p><p>除此以外，部分开发版根本没有预留用来调试的串口，在这种情况下调试更是一种折磨。此刻我们迫切地希望有工具能解决这个问题，而这个工具就是<code>RTT(Real Time Transfer)</code>。</p><h2 id="RTT的介绍"><a href="#RTT的介绍" class="headerlink" title="RTT的介绍"></a><code>RTT</code>的介绍</h2><p><code>RTT</code>是<code>SEGGER</code>公司开发的一种技术，它允许在嵌入式应用程序中进行系统监控和交互式用户输入&#x2F;输出。</p><p>相对于其他方案，<code>RTT</code>的优势很大：</p><ul><li>无需额外的接口，仅仅使用最基础的<code>SWDIO</code>和<code>SWCLK</code>便能使用。</li><li>速度十分快，几乎没有任何性能开销，因为其工作方式就是读写内存。</li><li>双向通信，主机与MCU之间可以互相收发数据。</li><li>多通道支持</li></ul><p><code>RTT</code>的原理就是<code>RTT</code>库在<code>RAM</code>中创建了一个名为<code>SEGGER RTT Control Block</code>的控制块结构。在发送和读取数据时，本质上是对这块区域进行写和读。</p><h2 id="RTT的使用"><a href="#RTT的使用" class="headerlink" title="RTT的使用"></a><code>RTT</code>的使用</h2><h3 id="RTT库的导入"><a href="#RTT库的导入" class="headerlink" title="RTT库的导入"></a><code>RTT</code>库的导入</h3><p>首先我们需要到<code>JLink</code>驱动的安装目录下的<code>Samples</code>文件夹中，在我的电脑上，这一路径为<code>C:\Program Files\SEGGER\JLink\Samples</code>。</p><p>在这个文件夹内部，有一个名为<code>RTT</code>的文件夹。新版的<code>JLink</code>驱动中没有这个文件夹，可以卸载然后安装旧版驱动来使用。</p><p><code>RTT</code>文件夹中有一个压缩包，解压，然后将其中的<code>Config</code>文件夹和<code>RTT</code>文件夹复制到自己的项目下，并添加到项目资源。此时便可以使用了。</p><h3 id="RTT库的用法"><a href="#RTT库的用法" class="headerlink" title="RTT库的用法"></a><code>RTT</code>库的用法</h3><p>在需要使用到<code>RTT</code>的地方，导入<code>SEGGER_RTT.h</code>。在调用<code>RTT</code>的<code>API</code>时，其会自动检测<code>RTT</code>是否初始化并进行初始化，所以不用显性的调用<code>SEGGER_RTT_Init</code>函数进行初始化。</p><h4 id="发送"><a href="#发送" class="headerlink" title="发送"></a>发送</h4><p><code>RTT</code>一共有三种向终端发送数据的方式，分别为<code>SEGGER_RTT_Write</code>、<code>SEGGER_RTT_WriteString</code>、<code>SEGGER_RTT_printf</code>。</p><p>说是发送其实并不准确，实际上是将数据写入到上行缓冲区。</p><p>如同函数名，这三者分别为将字节、字符串、以及格式化字符串写入到缓冲区。示例代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">uint8_t</span> bytes[] = <span class="hljs-string">&quot;Hello, World from SEGGER_RTT_Write!\n&quot;</span>;<br>SEGGER_RTT_Write(<span class="hljs-number">0</span>, bytes, <span class="hljs-built_in">strlen</span>(bytes));<br>SEGGER_RTT_WriteString(<span class="hljs-number">0</span>, <span class="hljs-string">&quot;Hello, World from SEGGER_RTT_WriteString!\n&quot;</span>);<br>SEGGER_RTT_printf(<span class="hljs-number">0</span>, <span class="hljs-string">&quot;Hello, World from SEGGER_RTT_printf on %d/%d/%d!\n&quot;</span>, year, month, day);<br></code></pre></td></tr></table></figure><p>将数据写入到缓冲区后，主机会读取缓冲区数据。</p><h5 id="添加SEGGER-RTT-printf对浮点数的输出支持"><a href="#添加SEGGER-RTT-printf对浮点数的输出支持" class="headerlink" title="添加SEGGER_RTT_printf对浮点数的输出支持"></a>添加<code>SEGGER_RTT_printf</code>对浮点数的输出支持</h5><p><code>RTT</code>库提供的<code>SEGGER_RTT_printf</code>函数默认并不支持浮点数，我们可以手动为其加上相关支持。</p><p>我们在库文件中找到<code>SEGGER_RTT_printf.c</code>文件。在其中的<code>SEGGER_RTT_vprintf</code>函数中的<code>switch-case</code>语句块的末尾，加上如下代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;f&#x27;</span>:<br><span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;F&#x27;</span>:&#123;<br>    <span class="hljs-type">float</span> fv;<br>    fv = (<span class="hljs-type">float</span>)va_arg(*pParamList, <span class="hljs-type">double</span>);<br><br>    <span class="hljs-comment">// 输出负号</span><br>    <span class="hljs-keyword">if</span> (fv &lt; <span class="hljs-number">0</span>) _StoreChar(&amp;BufferDesc, <span class="hljs-string">&#x27;-&#x27;</span>);<br><br>    v = <span class="hljs-built_in">abs</span>((<span class="hljs-type">int</span>)fv);<br><br>    _PrintInt(&amp;BufferDesc, v, <span class="hljs-number">10u</span>, NumDigits, FieldWidth, FormatFlags); <span class="hljs-comment">// 输出整数部分</span><br>    _StoreChar(&amp;BufferDesc, <span class="hljs-string">&#x27;.&#x27;</span>); <span class="hljs-comment">// 小数点</span><br><br>    <span class="hljs-comment">// 输出小数部分（3位小数）</span><br>    <span class="hljs-comment">// 若需要修改小数位数，修改下面`1000`和`3`这两个常数即可。</span><br>    <span class="hljs-comment">// 例如修改为两位小数，只需要将`1000`改为`10^2`=`100`，将`3`改为`2`即可。</span><br>    v = <span class="hljs-built_in">abs</span>((<span class="hljs-type">int</span>)(fv * <span class="hljs-number">1000</span>));<br>    v = v % <span class="hljs-number">1000</span>;<br>    _PrintInt(&amp;BufferDesc, v, <span class="hljs-number">10u</span>, <span class="hljs-number">3</span>, FieldWidth, FormatFlags);<br><br>    <span class="hljs-keyword">break</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>不过，除了资源紧张以外，建议直接使用<code>printf</code>函数转发<code>RTT</code>输出。</p><h5 id="printf转发RTT"><a href="#printf转发RTT" class="headerlink" title="printf转发RTT"></a><code>printf</code>转发<code>RTT</code></h5><p>在<code>JLink</code>驱动下的<code>RTT</code>文件夹内，还有一个名为<code>Syscalls</code>的文件夹。其中有四个文件，需要根据自身编译器类型选择合适的文件。</p><p>以<code>GCC</code>编译器为例，我们选择<code>SEGGER_RTT_Syscalls_GCC.c</code>文件复制到项目中合适路径下，并添加为项目资源。</p><p>然后在文件中调用<code>printf</code>函数并编译，我们就能观察到效果了。注意<code>printf</code>的内容后面一定要加上<code>\n</code>，防止内容卡在缓冲区无法输出。</p><p>此时我们会发现，<code>printf</code>的大部分功能我们都能使用，但还是不能输出浮点数。这一点我们只需要在编译选项后面加上</p><figure class="highlight diff"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs diff"><span class="hljs-deletion">-Wl,-u_printf_float</span><br></code></pre></td></tr></table></figure><p>即可。</p><h5 id="带有样式的字符串输出"><a href="#带有样式的字符串输出" class="headerlink" title="带有样式的字符串输出"></a>带有样式的字符串输出</h5><p>在<code>SEGGER_RTT.h</code>的文件末尾，可以看到许多基于<code>ANSI</code>的转义代码的宏。</p><p>要输出样式，我们可以直接用<code>ANSI</code>转义代码实现，也可以用预先设置好的宏。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-built_in">printf</span>(RTT_CTRL_TEXT_RED RTT_CTRL_BG_YELLOW <span class="hljs-string">&quot;Text color red, background blue\n&quot;</span>);<br></code></pre></td></tr></table></figure><h4 id="接收"><a href="#接收" class="headerlink" title="接收"></a>接收</h4><p>接收和发送类似，也存在一个下行缓冲区。与接收相关的函数分别为<code>SEGGER_RTT_Read()</code>、<code>SEGGER_RTT_HasKey()</code>、<code>SEGGER_RTT_GetKey()</code>。</p><p>如同函数名，其作用分别为将下行缓冲区的数据读入、检测下行缓冲区是否有数据以及获取其中的一位数据。<br>可以从下面的例子中了解其用法。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">char</span> acIn[<span class="hljs-number">4</span>];<br><span class="hljs-type">uint8_t</span> NumBytes = <span class="hljs-keyword">sizeof</span>(acIn);<br>NumBytes = SEGGER_RTT_Read(<span class="hljs-number">0</span>, &amp;acIn[<span class="hljs-number">0</span>], NumBytes); <span class="hljs-comment">// SEGGER_RTT_Read函数的返回值为缓冲区中的有效位数。</span><br><span class="hljs-keyword">if</span> (NumBytes) &#123;<br>  AnalyzeInput(acIn);<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">if</span> (SEGGER_RTT_HasKey()) &#123;<br>  <span class="hljs-type">int</span> c = SEGGER_RTT_GetKey();<br>&#125;<br></code></pre></td></tr></table></figure><p>一个完整的接收示例如下：</p><p>首先定义缓冲数组。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">char</span> RTT_BUFFER_DOWN[BUFFER_SIZE_DOWN];<br></code></pre></td></tr></table></figure><p>然后声明并实现接收函数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">RTT_ProcessDown</span><span class="hljs-params">()</span>;<br><br><span class="hljs-type">void</span> <span class="hljs-title function_">RTT_ProcessDown</span><span class="hljs-params">()</span><br>&#123;<br>    <span class="hljs-type">uint8_t</span> NumBytes = BUFFER_SIZE_DOWN;<br>    NumBytes = SEGGER_RTT_Read(<span class="hljs-number">0</span>, &amp;RTT_BUFFER_DOWN, NumBytes);<br>    <span class="hljs-keyword">if</span> (NumBytes)<br>    &#123;<br>        <span class="hljs-comment">// 手动添加字符串终止符</span><br>        RTT_BUFFER_DOWN[NumBytes] = <span class="hljs-string">&#x27;\0&#x27;</span>;<br><br>        <span class="hljs-comment">// 数据处理</span><br>        <span class="hljs-built_in">printf</span>(RTT_CTRL_RESET <span class="hljs-string">&quot;%d\n&quot;</span>, atoi(RTT_BUFFER_DOWN));<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>最后在主循环中调用。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">while</span> (<span class="hljs-number">1</span>)<br>&#123;<br>    <span class="hljs-comment">/* USER CODE END WHILE */</span><br>    <span class="hljs-comment">/* USER CODE BEGIN 3 */</span><br>    RTT_ProcessDown();<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="终端切换"><a href="#终端切换" class="headerlink" title="终端切换"></a>终端切换</h4><h4 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h4>]]></content>
    
    
    
    <tags>
      
      <tag>嵌入式</tag>
      
      <tag>工具</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>个人博客的重新搭建历程</title>
    <link href="/2026/03/02/%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E7%9A%84%E9%87%8D%E6%96%B0%E6%90%AD%E5%BB%BA%E5%8E%86%E7%A8%8B/"/>
    <url>/2026/03/02/%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E7%9A%84%E9%87%8D%E6%96%B0%E6%90%AD%E5%BB%BA%E5%8E%86%E7%A8%8B/</url>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>回想起来先前的个人博客已经有许多年了，但当时的我还不成熟，许多方面都是一知半解、抱着玩的心态在做。<br>当时对于<code>Git</code>的使用远远不熟悉，没有想到将原来博客的原始资料用<code>Git</code>保存，现在已经不知道位于何处了。<br>我想正好借此机会，重建整个博客，并记录自己的学习记录、笔记、日常等真正有意义的事物。</p><h2 id="Hexo的搭建"><a href="#Hexo的搭建" class="headerlink" title="Hexo的搭建"></a><code>Hexo</code>的搭建</h2><p>既然想要重建博客，那自然是要从博客的基本框架开始。<code>Hexo</code>是一个不错的博客框架，功能和生态都十分丰富，十分适合使用。</p><h3 id="环境配置"><a href="#环境配置" class="headerlink" title="环境配置"></a>环境配置</h3><p><code>Hexo</code>基于<code>Node.js</code>运行。那自然我们需要到<a href="https://nodejs.org/zh-cn"><code>Node.js</code>官网</a>下载并安装<code>Node.js</code>。</p><h3 id="Hexo安装"><a href="#Hexo安装" class="headerlink" title="Hexo安装"></a><code>Hexo</code>安装</h3><p>首先我们需要安装<code>hexo-cli</code>，这是我们用来创建<code>Hexo</code>工程的工具。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install -g hexo-cli<br></code></pre></td></tr></table></figure><p>这之后，我们就能创建<code>Hexo</code>工程了。我们要先找到一个合适的目录，然后执行下面命令。此处的<code>your-blog-name</code>请更换为你希望的文件夹名称。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo init your-blog-name<br><span class="hljs-built_in">cd</span> your-blog-name<br>npm install<br></code></pre></td></tr></table></figure><p>执行完后就会发现目录下多了一个名为<code>your-blog-name</code>的文件夹，里面就是整个<code>Hexo</code>博客工程。</p><h3 id="Hexo的使用"><a href="#Hexo的使用" class="headerlink" title="Hexo的使用"></a><code>Hexo</code>的使用</h3><p>我们使用<code>Hexo</code>一般有下面4个用途：</p><ol><li><p>创建新文章，<code>post-title</code>处修改为博客的标题。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo new <span class="hljs-string">&quot;post-title&quot;</span><br></code></pre></td></tr></table></figure></li><li><p>生成静态文件</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo generate<br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo g<br></code></pre></td></tr></table></figure></li><li><p>本地部署</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo server<br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo s<br></code></pre></td></tr></table></figure></li><li><p>部署到<code>GitHub</code></p><p>需要先安装<code>hexo-deployer-git</code>以使<code>Hexo</code>支持将博客部署到远程<code>Git</code>仓库中。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install hexo-deployer-git --save<br></code></pre></td></tr></table></figure><p>然后修改项目下的配置文件<code>_config.yml</code>。<br>找到<code>deploy</code>项，若无则自行创建，然后按照下面说明进行修改。</p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs yml"><span class="hljs-comment"># Deployment</span><br><span class="hljs-comment">## Docs: https://hexo.io/docs/one-command-deployment</span><br><span class="hljs-attr">deploy:</span><br>  <span class="hljs-attr">type:</span> <span class="hljs-string">&#x27;git&#x27;</span><br>  <span class="hljs-attr">repo:</span> <span class="hljs-string">https://github.com/username/username.github.io.git</span> <span class="hljs-comment"># GitHub Pages 项目路径</span><br>  <span class="hljs-attr">branch:</span> <span class="hljs-string">master</span>  <span class="hljs-comment"># 注意：新仓库默认分支可能是 main 而非 master</span><br></code></pre></td></tr></table></figure><p>最后使用下面命令即可。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo deploy<br></code></pre></td></tr></table></figure><p>或</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo d<br></code></pre></td></tr></table></figure></li></ol><h2 id="主题的安装和配置"><a href="#主题的安装和配置" class="headerlink" title="主题的安装和配置"></a>主题的安装和配置</h2><p>我在互联网上浏览了一圈，发现<code>Hexo</code>的主题和当年并无太大差异。我当时选择的<code>Fluid</code>主题目前来看仍是相对最美观、功能最丰富的主题。</p><p><code>Fluid</code>主题的安装方法详见其<a href="https://github.com/fluid-dev/hexo-theme-fluid/">项目地址</a>。安装后只要按照配置文件内部的说明按需修改即可。</p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
